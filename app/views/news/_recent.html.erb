<h2><%= t("news.recent_title")%></h2>
<ul>
	<% for news in News.recent(3) %>
		<li><%= link_to news.name, news_path(news.permalink)%></li>
	<%end%>
</ul>
<p>
	<%= link_to t("news.see_archive"), news_all_path %>
</p>
